import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  root: {
    display: 'flex',
    width: '100%',
    background: '#f3f4ed',
    flexWrap: 'wrap',
    justifyContent: 'center',
  },
}), {
  name: 'IndexLayout',
});